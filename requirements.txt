# quant-learning 项目依赖
#
# 第一部分：申万行业分析（shenwan_industry/，不依赖 vnpy）
# 在任意 Python 3.10+ 环境安装下列依赖即可（含桌面窗口 GUI 的 PySide6）：
fastapi
uvicorn
pydantic
tushare
pandas
# GUI（申万桌面窗口 desktop.pyw 使用 Qt WebEngine 需要 PySide6）
PySide6

# 第二部分：vnpy 生态（holders / vnpy_examples）
# 必须使用 vnpy 客户端（veighna studio）自带的 Python 运行（--system-site-packages
# 继承 vnpy / vnpy_tushare / vnpy_ctastrategy / tushare / pandas / numpy / Pillow /
# ta-lib 等全部依赖），无需（也请勿）用 pip 单独安装这些库。
#
# 安装方式见 README.md（按命令逐行执行即可）。
